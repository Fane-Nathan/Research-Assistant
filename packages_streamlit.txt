# Minimal system packages for Streamlit Cloud
# Remove Playwright dependencies since we're using aiohttp for PDF fetching

# Keep only essential system libraries
libfontconfig1

# Removed all Playwright browser dependencies:
# libnss3, libnspr4, libdbus-glib-1-2, libatk1.0-0, libatk-bridge2.0-0
# libcups2, libdrm2, libgbm1, libexpat1, libxcb1, libxkbcommon0
# libatspi2.0-0, libxrandr2, libxcomposite1, libxcursor1, libxdamage1
